name: Build A8E

on:
  push:
    branches: ["main", "master"]
    tags: ["v*"]
  pull_request:
    branches: ["main", "master"]
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Existing tag to build/publish (e.g. v1.1.1). Leave empty for build-only run."
        required: false
        type: string

env:
  A8E_RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}

jobs:
  build-linux:
    name: Linux (Ubuntu)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref }}

      - name: Install Dependencies
        run: sudo apt-get update && sudo apt-get install -y cmake libsdl2-dev

      - name: Configure and Build
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build -j4

      - name: Package standalone bundle (tags only)
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && startsWith(github.event.inputs.release_tag, 'v'))
        run: |
          bundle="dist/A8E-${A8E_RELEASE_TAG}-linux"
          mkdir -p "$bundle"
          cp build/A8E/A8E "$bundle/A8E"
          chmod +x "$bundle/A8E"

          SDL_SO="$(ldd build/A8E/A8E | awk '/libSDL2-2\.0\.so\.0/{print $3; exit}')"
          if [ -z "$SDL_SO" ] || [ ! -f "$SDL_SO" ]; then
            echo "Failed to resolve libSDL2-2.0.so.0 dependency from build output"
            ldd build/A8E/A8E
            exit 1
          fi
          cp "$SDL_SO" "$bundle/libSDL2-2.0.so.0"

          printf '%s\n' \
            '#!/usr/bin/env sh' \
            'DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"' \
            'export LD_LIBRARY_PATH="$DIR${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"' \
            'exec "$DIR/A8E" "$@"' \
            > "$bundle/A8E.sh"
          chmod +x "$bundle/A8E.sh"

          LD_LIBRARY_PATH="$bundle${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}" ldd "$bundle/A8E" | tee "$bundle/ldd.txt"
          if grep -q "not found" "$bundle/ldd.txt"; then
            echo "Standalone Linux bundle has unresolved runtime dependencies"
            exit 1
          fi

          tar -C dist -czf "dist/A8E-${A8E_RELEASE_TAG}-linux.tar.gz" "A8E-${A8E_RELEASE_TAG}-linux"

      - name: Upload artifact (tags only)
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && startsWith(github.event.inputs.release_tag, 'v'))
        uses: actions/upload-artifact@v4
        with:
          name: A8E-linux
          path: dist/A8E-${{ env.A8E_RELEASE_TAG }}-linux.tar.gz

  build-macos:
    name: macOS (Homebrew)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref }}

      - name: Install Dependencies
        run: brew install cmake sdl2

      - name: Configure and Build
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build -j4

      - name: Package standalone bundle (tags only)
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && startsWith(github.event.inputs.release_tag, 'v'))
        run: |
          bundle="dist/A8E-${A8E_RELEASE_TAG}-macos"
          mkdir -p "$bundle"
          cp build/A8E/A8E "$bundle/A8E"
          chmod +x "$bundle/A8E"

          SDL_DYLIB_PATH="$(otool -L build/A8E/A8E | awk '/libSDL2-2\.0\.0\.dylib/{print $1; exit}')"
          if [ -z "$SDL_DYLIB_PATH" ] || [ ! -f "$SDL_DYLIB_PATH" ]; then
            echo "Failed to resolve libSDL2-2.0.0.dylib dependency from build output"
            otool -L build/A8E/A8E
            exit 1
          fi
          cp "$SDL_DYLIB_PATH" "$bundle/libSDL2-2.0.0.dylib"

          install_name_tool -change "$SDL_DYLIB_PATH" "@executable_path/libSDL2-2.0.0.dylib" "$bundle/A8E"

          otool -L "$bundle/A8E" | tee "$bundle/otool.txt"
          if ! grep -q "@executable_path/libSDL2-2.0.0.dylib" "$bundle/otool.txt"; then
            echo "Standalone macOS bundle did not rewrite SDL load path"
            exit 1
          fi
          if grep -Eq '/opt/homebrew/opt/sdl2|/usr/local/(opt|Cellar)/sdl2' "$bundle/otool.txt"; then
            echo "Standalone macOS bundle still references Homebrew SDL path"
            exit 1
          fi

          tar -C dist -czf "dist/A8E-${A8E_RELEASE_TAG}-macos.tar.gz" "A8E-${A8E_RELEASE_TAG}-macos"

      - name: Upload artifact (tags only)
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && startsWith(github.event.inputs.release_tag, 'v'))
        uses: actions/upload-artifact@v4
        with:
          name: A8E-macos
          path: dist/A8E-${{ env.A8E_RELEASE_TAG }}-macos.tar.gz

  build-macos-arm64:
    name: macOS (Apple Silicon)
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref }}

      - name: Install Dependencies
        run: brew install cmake sdl2

      - name: Configure and Build
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DCMAKE_OSX_ARCHITECTURES=arm64
          cmake --build build -j4

      - name: Package standalone bundle (tags only)
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && startsWith(github.event.inputs.release_tag, 'v'))
        run: |
          bundle="dist/A8E-${A8E_RELEASE_TAG}-macos-arm64"
          mkdir -p "$bundle"
          cp build/A8E/A8E "$bundle/A8E"
          chmod +x "$bundle/A8E"

          SDL_DYLIB_PATH="$(otool -L build/A8E/A8E | awk '/libSDL2-2\.0\.0\.dylib/{print $1; exit}')"
          if [ -z "$SDL_DYLIB_PATH" ] || [ ! -f "$SDL_DYLIB_PATH" ]; then
            echo "Failed to resolve libSDL2-2.0.0.dylib dependency from build output"
            otool -L build/A8E/A8E
            exit 1
          fi
          cp "$SDL_DYLIB_PATH" "$bundle/libSDL2-2.0.0.dylib"

          install_name_tool -change "$SDL_DYLIB_PATH" "@executable_path/libSDL2-2.0.0.dylib" "$bundle/A8E"

          otool -L "$bundle/A8E" | tee "$bundle/otool.txt"
          if ! grep -q "@executable_path/libSDL2-2.0.0.dylib" "$bundle/otool.txt"; then
            echo "Standalone macOS arm64 bundle did not rewrite SDL load path"
            exit 1
          fi
          if grep -Eq '/opt/homebrew/opt/sdl2|/usr/local/(opt|Cellar)/sdl2' "$bundle/otool.txt"; then
            echo "Standalone macOS arm64 bundle still references Homebrew SDL path"
            exit 1
          fi

          tar -C dist -czf "dist/A8E-${A8E_RELEASE_TAG}-macos-arm64.tar.gz" "A8E-${A8E_RELEASE_TAG}-macos-arm64"

      - name: Upload artifact (tags only)
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && startsWith(github.event.inputs.release_tag, 'v'))
        uses: actions/upload-artifact@v4
        with:
          name: A8E-macos-arm64
          path: dist/A8E-${{ env.A8E_RELEASE_TAG }}-macos-arm64.tar.gz

  build-windows-msvc:
    name: Windows (MSVC)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref }}

      - name: Install SDL2 via vcpkg
        # Note: vcpkg is pre-installed on GitHub's windows-latest runners!
        run: vcpkg install sdl2:x64-windows-static

      - name: Configure CMake
        run: >
          cmake -S . -B build/msvc
          -G "Visual Studio 17 2022" -A x64
          -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_INSTALLATION_ROOT\scripts\buildsystems\vcpkg.cmake"
          -DVCPKG_TARGET_TRIPLET=x64-windows-static
          -DCMAKE_MSVC_RUNTIME_LIBRARY="MultiThreaded$<$<CONFIG:Debug>:Debug>"

      - name: Build
        run: cmake --build build/msvc --config Release

      - name: Package binary (tags only)
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && startsWith(github.event.inputs.release_tag, 'v'))
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item "build/msvc/A8E/Release/A8E.exe" "dist/A8E-$env:A8E_RELEASE_TAG-windows-msvc.exe"

      - name: Upload artifact (tags only)
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && startsWith(github.event.inputs.release_tag, 'v'))
        uses: actions/upload-artifact@v4
        with:
          name: A8E-windows-msvc
          path: dist/A8E-${{ env.A8E_RELEASE_TAG }}-windows-msvc.exe

  build-windows-mingw:
    name: Windows (MinGW / MSYS2)
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref }}

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-make
            mingw-w64-x86_64-SDL2

      - name: Configure and Build
        run: |
          cmake -S . -B build/mingw -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release -DCMAKE_EXE_LINKER_FLAGS="-static"
          cmake --build build/mingw -j4

      - name: Package binary (tags only)
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && startsWith(github.event.inputs.release_tag, 'v'))
        run: |
          mkdir -p dist
          cp build/mingw/A8E/A8E.exe "dist/A8E-${A8E_RELEASE_TAG}-windows-mingw.exe"

      - name: Upload artifact (tags only)
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && startsWith(github.event.inputs.release_tag, 'v'))
        uses: actions/upload-artifact@v4
        with:
          name: A8E-windows-mingw
          path: dist/A8E-${{ env.A8E_RELEASE_TAG }}-windows-mingw.exe

  publish-release:
    name: Publish GitHub Release
    if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && startsWith(github.event.inputs.release_tag, 'v'))
    needs:
      - build-linux
      - build-macos
      - build-macos-arm64
      - build-windows-msvc
      - build-windows-mingw
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets

      - name: Create/Update Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.A8E_RELEASE_TAG }}
          generate_release_notes: true
          files: |
            release-assets/A8E-linux/A8E-${{ env.A8E_RELEASE_TAG }}-linux.tar.gz
            release-assets/A8E-macos/A8E-${{ env.A8E_RELEASE_TAG }}-macos.tar.gz
            release-assets/A8E-macos-arm64/A8E-${{ env.A8E_RELEASE_TAG }}-macos-arm64.tar.gz
            release-assets/A8E-windows-msvc/A8E-${{ env.A8E_RELEASE_TAG }}-windows-msvc.exe
            release-assets/A8E-windows-mingw/A8E-${{ env.A8E_RELEASE_TAG }}-windows-mingw.exe
